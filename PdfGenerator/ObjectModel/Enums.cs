using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DocGen.ObjectModel
{
    public enum HorizontalAlignment { Left, Center, Right };
    public enum VerticalAlignment { Top, Center, Bottom };
    public enum FontFormats { Bold, Italic, Underlined }
    public enum ElementWidth { Absolute, Percentage }
    public enum TableBorders { None, Single }
    public enum Justification { Left, Center, Right, Justified }
    public enum HeadingType { H1, H2, H3, H4, H5, H6}
}

